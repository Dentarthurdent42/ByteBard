node_modules/
test-results/
.cert/
.env

# large models for the optional gesture-image + pose-bench tests (auto/manually fetched)
tests/gesture-img/hand_landmarker.task
tests/pose-bench/models/

# Written at deploy time by scripts/stamp.mjs — a property of a deploy,
# not of the source.
build.json

# A memo recording which source state scripts/screenshot-sync.mjs last checked,
# so an unchanged tree costs a hash instead of a browser launch. Local, because
# committing it would create a second thing to keep in sync.
.screenshot-cache
