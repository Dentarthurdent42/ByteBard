name: UI/UX Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ui-ux:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Contrast check
        run: npm run test:contrast

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      # The guided tour is data pointing at live UI — this fails the build if
      # a UI change orphans a tutorial step (see src/ui/tutorial.js header).
      - name: Tutorial staleness guard
        run: npm run test:tutorial

      # Proves a redeploy is visible on the next load. Regression guard for the
      # service worker serving stale builds to returning visitors.
      - name: Service-worker freshness
        run: npm run test:sw

      # The engine starts with the page, muted. Forces the suspended
      # AudioContext that real browsers hand you and headless Chromium does
      # not, because that difference already hid one shipped bug.
      - name: Audio launch (muted, suspended context)
        run: npm run test:launch

      # Header + panel geometry at every breakpoint, camera off AND on. The
      # camera-on state is where three header bugs shipped unseen.
      - name: Layout guard
        run: npm run test:layout

      # DISABLED — the Claude Vision screenshot pass. The deterministic guards
      # above (contrast, tutorial, service worker, audio launch, layout) still
      # run on every PR and are what actually gate a merge; this step needs an
      # API key, costs a call per viewport, and grades screenshots by
      # judgement rather than assertion. Re-enable by removing the `if: false`
      # (and the same line on the upload step below).
      - name: Run UI/UX tests
        if: false
        run: npm run test:ui
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Upload report
        if: false
        uses: actions/upload-artifact@v4
        with:
          name: ui-ux-report
          path: test-results/ui-ux-report.html
          retention-days: 30
